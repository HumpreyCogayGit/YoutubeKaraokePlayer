# Server Environment Variables

# Database Configuration
# For local development, use PostgreSQL connection string
# Format: postgresql://username:password@localhost:5432/database_name
# For production: Use your cloud database provider (Vercel Postgres, Neon, Supabase, etc.)
DATABASE_URL=postgresql://username:password@localhost:5432/karaoke_playlists

# Google OAuth Credentials
# Get from: https://console.cloud.google.com/ → Credentials → OAuth 2.0 Client IDs
# NEVER commit these values - they should only be in .env (ignored by git)
GOOGLE_CLIENT_ID=your_google_client_id_here
GOOGLE_CLIENT_SECRET=your_google_client_secret_here
GOOGLE_CALLBACK_URL=http://localhost:5000/auth/google/callback

# Session Secret - REQUIRED (minimum 32 characters)
# Generate a strong secret: openssl rand -base64 32
# CRITICAL: Use different secrets for development and production
# NEVER commit the actual secret to version control
SESSION_SECRET=your_random_session_secret_here_change_in_production_MINIMUM_32_CHARS

# Server Configuration
PORT=5000

# Frontend URL (for CORS)
# For local development: http://localhost:3000
# For production: https://your-app.vercel.app
CLIENT_URL=http://localhost:3000

# Node Environment
NODE_ENV=development
